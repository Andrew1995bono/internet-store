export interface ProductCard {
  productTitle: string;
  description: string;
  freshness: string;
  farm: string;
  delivery: string;
  stock: number;
  priceUsual: number;
  pricePromotional: number;
}
